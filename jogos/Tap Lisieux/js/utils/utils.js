// ═══════════════════════════════════════════════════════
//  UTILS.JS
//  Funções utilitárias gerais:
//  - formatarNum() — 1500 → "1.5K"
//  - clamp() — limita valor entre min e max
//  - lerp() — interpolação linear
//  - aleatorio() — número aleatório entre min e max
//  - estaVisivel() — checa se ponto está no canvas
//  - escaparHTML() — previne XSS
//
//  Depende de: (nenhum — módulo base)
//  Usado por: TODOS os módulos
// ═══════════════════════════════════════════════════════

"use strict";

const Utils = (() => {
    // TODO: formatarNum, clamp, lerp, aleatorio, estaVisivel, escaparHTML
})();
